import styled from '@emotion/styled';

const SurfaceSubheader = styled.div`
  padding: 1rem 1rem 0 1rem;
  font-size: 0.9rem;
  font-weight: 200;
  color: ${({ theme }) => theme.colors.textSecondary.toString()};
`;

export default SurfaceSubheader;
