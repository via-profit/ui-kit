{
  "name": "@via-profit/ui-kit-monorepo",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run start --workspace packages/ui-kit-website",
    "build": "npm run build --workspace packages/ui-kit"
  },
  "author": "",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^8.2.1",
    "fs-extra": "^11.1.1"
  }
}
